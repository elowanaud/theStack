import { LoginForm } from "@/features/auth/login/components/LoginForm";

export default function LoginPage() {
	return (
		<main className="m-auto flex min-h-screen max-w-sm items-center">
			<LoginForm />
		</main>
	);
}
